import useWeb3 from "hooks/useWeb3";
import useSWR from "swr";
import { sdk } from ".";

const useExhausted = (airdropId: string, address?: string | null) => {
  // check how much owed and if enough balance left
  /// .... todo
  return false
};

export default useExhausted;
